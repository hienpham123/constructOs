# Server Configuration
PORT=2222
NODE_ENV=development

# API Base URL (for avatar URLs)
# Development: http://localhost:2222
# Production: https://your-api-domain.com
API_BASE_URL=http://localhost:2222
# Alternative: SERVER_URL=http://localhost:2222

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=constructos-secret-key-change-this-in-production

# MySQL Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=constructos_user
DB_PASSWORD=your_secure_password_here
DB_NAME=constructOS
DB_CONNECTION_LIMIT=10

# Instructions:
# 1. Copy this file to .env: cp env.example.txt .env
# 2. Update DB_USER, DB_PASSWORD with your MySQL credentials
# 3. Make sure MySQL database 'constructos' is already created
# 4. For production: Set API_BASE_URL to your production domain
# 5. For production: Change JWT_SECRET to a strong, unique secret
