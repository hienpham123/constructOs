# Server Configuration
PORT=2222
NODE_ENV=development

# API Base URL (for avatar URLs)
# Development: http://localhost:2222
# Production: https://your-api-domain.com
API_BASE_URL=http://localhost:2222
# Alternative: SERVER_URL=http://localhost:2222

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=constructos-secret-key-change-this-in-production

# Database Configuration
# Database Type: 'mysql' or 'postgres' (auto-detected if not set)
# - Local: MySQL (port 3306)
# - Dev/Staging: PostgreSQL (Supabase, Render, Railway)
# - Production: MySQL
# DB_TYPE=mysql  # Optional: explicitly set database type

# MySQL Database Configuration (for local and production)
DB_HOST=localhost
DB_PORT=3306
DB_USER=constructos_user
DB_PASSWORD=your_secure_password_here
DB_NAME=constructOS
DB_CONNECTION_LIMIT=10

# PostgreSQL Configuration (for dev/staging with Supabase)
# DB_HOST=your-supabase-host.supabase.co
# DB_PORT=5432
# DB_USER=postgres.your-project-ref
# DB_PASSWORD=your-supabase-password
# DB_NAME=postgres
# DB_SSL=true

# Email Configuration (FREE, không giới hạn)
# Gmail: EMAIL_SERVICE=gmail, SMTP_USER=your-email@gmail.com, SMTP_PASS=app-password
# Outlook: SMTP_HOST=smtp-mail.outlook.com, SMTP_PORT=587
EMAIL_SERVICE=gmail
EMAIL_FROM=noreply@yourdomain.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Instructions:
# 1. Copy this file to .env: cp env.example.txt .env
# 2. Update DB_USER, DB_PASSWORD with your MySQL credentials
# 3. Make sure MySQL database 'constructos' is already created
# 4. For production: Set API_BASE_URL to your production domain
# 5. For production: Change JWT_SECRET to a strong, unique secret
# 6. Cấu hình Email để nhận thông báo (FREE, không giới hạn) - Xem FREE_NOTIFICATION_SETUP.md
